# Spacecraft-System
A Raspberry Pi measures distance using the ultrasonic sensor, displays the distance on the LCD, and runs a small spacecraft-style monitoring system that detects an obstacle, reports telemetry, and enters a safe state when the distance is too small.
